# AI2BIZ Telegram Bot 🤖

AI2BIZ Bot — это продвинутый Telegram-бот для автоматизации продаж и квалификации лидов. Бот реализует автоворонку продаж с запланированными сообщениями ("дожимами"), интеграцию с Google Sheets и запись на консультации.

## 🚀 Возможности

- **Автоворонка продаж**: Последовательная отправка прогревающих сообщений и материалов (кейсы, чек-листы) с заданными интервалами (через 10 мин, 1 час, 24 часа).
- **Scheduler (Планировщик)**: Автоматическая отправка follow-up сообщений даже если пользователь молчит.
- **Интеграция с Google Sheets**: 
  - Сохранение данных пользователей и истории их действий.
  - Логирование отправленных сообщений.
  - Экспорт ответов из форм квалификации.
- **Квалификация лидов**: Пошаговая анкета для оценки качества лида перед записью на консультацию.
- **Валидация данных**: Проверка корректности ввода имени, телефона и ответов на открытые вопросы.
- **Рассылки**: Возможность отправки сообщений всем пользователям из админки.

## 🛠 Установка и запуск

### Предварительные требования

- Python 3.8+
- Учётная запись Google Cloud для доступа к Sheets API (файл `credentials.json`, если используется Google Sheets).
- Токен Telegram бота от @BotFather.

### 1. Клонирование репозитория

```bash
git clone https://github.com/Start-Zero-To-One/ai2biz-bot.git
cd ai2biz-bot
```

### 2. Настройка виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Для macOS/Linux
# venv\Scripts\activate   # Для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта и добавьте следующие переменные:

```env
TOKEN=ваш_токен_бота
ADMIN_CHAT_ID=ваш_id_администратора
GOOGLE_SHEETS_ID=id_вашей_таблицы
GOOGLE_SERVICE_ACCOUNT_JSON='{...весь_json_ключ_одной_строкой...}'
```

### 5. Запуск бота

```bash
python main.py
```

## 📂 Структура проекта

- `main.py` — Точка входа, обработчики команд и логика бота.
- `messages.py` — Тексты всех сообщений и конфигурация воронки.
- `scheduler_manager.py` — Логика планировщика задач (APScheduler) для автоворонки.
- `requirements.txt` — Список зависимостей.

## 📊 Google Sheets

Бот автоматически создает и обновляет следующие листы в таблице:
- **Users**: База всех пользователей с их статусами и метками времени.
- **Stats**: Лог действий пользователей.
- **Leads Files/Consultation**: Заявки на файлы и консультации.
- **Form Answers**: Ответы на квалификационную анкету.

## 🔄 Логика автоворонки

Бот отслеживает действия пользователя. Если пользователь "застревает" на определенном этапе, планировщик отправляет следующее сообщение через заданный интервал, побуждая к действию (запись на консультацию, скачивание файла и т.д.). Воронка останавливается автоматически, когда цель достигнута (заполнена заявка).
